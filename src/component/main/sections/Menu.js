import SectionWrapper from "../../../wrapper/SectionWrapper"

const Menu = () => {
    return (
        <SectionWrapper sectionClass={ "menu" }>

        </SectionWrapper>
    )
}

export default Menu;